import React from 'react';
import { View, StyleSheet } from 'react-native';

const AdvancedAccessIcon = () => {
  return (
    <View style={styles.container} />
  );
};

const styles = StyleSheet.create({
  container: {
    width: 24,
    height: 24,
    borderRadius: 12,
    backgroundColor: '#6366F1',
  },
});

export default AdvancedAccessIcon;
